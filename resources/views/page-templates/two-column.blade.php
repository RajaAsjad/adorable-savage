{{--
    Page template: Two Column

    Admin mapping (all from Pages dashboard):
    - featured image → left column
    - title → right column heading
    - description (editor) → right column HTML content
    - custom field "eyebrow" (optional) → small label above title
--}}
@php
    /** @var \App\Models\Page|null $page */
@endphp
