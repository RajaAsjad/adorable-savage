{{--
    Page template: Home

    Admin mapping (fully dynamic):
    - description (editor) → entire left hero (kicker, titles, intro, CTAs)
    - image (featured image) → right side hero photo
--}}
@php
    /** @var \App\Models\Page|null $page */
@endphp
